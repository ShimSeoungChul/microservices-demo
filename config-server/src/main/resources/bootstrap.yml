spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ShimSeoungChul/spring-cloud-config-server-repository.git
          default-label: main
          username: ShimSeoungChul
          password: ********
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: springCloud_Pwd!
